name: Test

on:
  workflow_dispatch

jobs:
  builds:
    runs-on: unbuntu-latest

    steps:
    - name: Test
      with:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "$GITHUB_TOKEN"
